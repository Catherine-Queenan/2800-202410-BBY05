<!--Template for the navbar that loads when a user is not logged in.
    Includes custom script for dynamic resizing.-->
<div class="container-fluid">
	<header class="d-flex justify-content-center py-3 mt-4">
		<h1 id="firstHeading" class="font-size-lg font-size-md font-size-sm display-3 fw-bold" style="font-kerning: auto; padding-left:10px">Paw</h1>
		<ul class="nav nav-pills">
			<li class="nav-item">

				<!--Home -->
				<form method="get" class="col-md-4 d-flex">
					<%- include('icon', {iconPath: '/icons/Logo.png', altText: 'Logo', action: '/', theClass: '', theAttributes: 'style= width:100px; height:100px;'}) %>
				</form>
			</li>
		</ul>
		<h1 id="secondHeading" class="font-size-lg font-size-md font-size-sm display-3 fw-bold" style="font-kerning: auto; padding-left:15px">folio</h1>
	</header>
</div>

<!--Script built with the help of AI, to change the sizing and spacing
    of the text based on the size of the window.-->
<script>
    function adjustLetterSpacing() {
        var windowWidth = window.innerWidth;
        var firstHeading = document.getElementById('firstHeading');
        var secondHeading = document.getElementById('secondHeading');
        var maxLetterSpacing = windowWidth * 0.5; // 50% of the window's width

        // Calculate letter spacing based on window width
        var letterSpacing = Math.min(0.025 * windowWidth, maxLetterSpacing);

        // Apply letter-spacing to headings
        firstHeading.style.letterSpacing = letterSpacing + 'px';
        secondHeading.style.letterSpacing = letterSpacing + 'px';
    }

    // Call the function initially and on window resize
    adjustLetterSpacing();
    window.addEventListener('resize', adjustLetterSpacing);
</script>