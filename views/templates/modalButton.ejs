<!--Template for all buttons used within modals.-->
<button formaction="<%= action %>" class="btn btn-primary <%= xClasses %>" data-toggle="modal" data-target= "<%=modalId%>">
    <%= content %>
</button>