<%- include("templates/header") %>
<div>

    <% if (errorMessage) { %>
        <div class="error-message"><%= errorMessage %></div>
    <% } %>
    <form action="/emailConfirmation" method="post">
        <label>Email</label>
        <input type="text" name="email">
        <button>Confirm</button>
    </form>
</div>
<%- include("templates/footerCopyright") %>